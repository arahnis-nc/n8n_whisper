import type { IExecuteFunctions } from 'n8n-workflow';
import { z } from 'zod';
export declare function parseOrThrow<T extends z.ZodTypeAny>(ctx: IExecuteFunctions, schema: T, raw: unknown, message: string, itemIndex: number): z.infer<T>;
