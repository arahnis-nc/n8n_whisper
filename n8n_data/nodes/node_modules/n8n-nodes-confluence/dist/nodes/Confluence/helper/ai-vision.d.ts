import type { IExecuteFunctions } from 'n8n-workflow';
export interface AIVisionConfig {
    provider: 'openai' | 'anthropic' | 'google' | 'custom';
    apiKey?: string;
    apiUrl?: string;
    model?: string;
    maxTokens?: number;
    temperature?: number;
}
export declare function generateImageDescription(ctx: IExecuteFunctions, imageData: ArrayBuffer, filename: string, config: AIVisionConfig): Promise<string>;
