"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.htmlToPlaintext = htmlToPlaintext;
exports.buildPagePlainText = buildPagePlainText;
function htmlToPlaintext(html) {
    if (!html)
        return '';
    let s = html
        .replace(/<br\s*\/>/gi, '\n')
        .replace(/<\/p>/gi, '\n\n')
        .replace(/<\/h[1-6]>/gi, '\n\n')
        .replace(/<li[^>]*>/gi, '- ')
        .replace(/<\/li>/gi, '\n')
        .replace(/<tr[^>]*>/gi, '\n')
        .replace(/<t[dh][^>]*>/gi, '\t');
    s = s
        .replace(/<ac:structured-macro[^>]*>/gi, '')
        .replace(/<\/ac:structured-macro>/gi, '')
        .replace(/<ac:layout[^>]*>/gi, '')
        .replace(/<\/ac:layout>/gi, '')
        .replace(/<ac:layout-section[^>]*>/gi, '')
        .replace(/<\/ac:layout-section>/gi, '')
        .replace(/<ac:layout-cell[^>]*>/gi, '')
        .replace(/<\/ac:layout-cell>/gi, '')
        .replace(/<ac:parameter[^>]*>.*?<\/ac:parameter>/gis, '')
        .replace(/<ac:link[^>]*>/gi, '')
        .replace(/<\/ac:link>/gi, '')
        .replace(/<ac:rich-text-body[^>]*>/gi, '')
        .replace(/<\/ac:rich-text-body>/gi, '')
        .replace(/<ri:[^>]*>/gi, '');
    s = s.replace(/<script[^>]*>[\s\S]*?<\/script>/gi, '');
    s = s.replace(/<style[^>]*>[\s\S]*?<\/style>/gi, '');
    s = s.replace(/<[^>]+>/g, ' ');
    s = s
        .replace(/&nbsp;/g, ' ')
        .replace(/&amp;/g, '&')
        .replace(/&lt;/g, '<')
        .replace(/&gt;/g, '>')
        .replace(/&quot;/g, '"')
        .replace(/&#39;/g, "'");
    s = s
        .replace(/[ \t]+/g, ' ')
        .replace(/\s*\n\s*/g, '\n')
        .trim();
    return s;
}
function buildPagePlainText(spaceName, spaceKey, title, bodyHtml) {
    const parts = [];
    if (spaceName)
        parts.push(`Space: ${spaceName} (${spaceKey})`);
    if (title)
        parts.push(`Titel: ${title}`);
    parts.push(htmlToPlaintext(bodyHtml || ''));
    return parts.join('\n\n');
}
//# sourceMappingURL=transformations.js.map